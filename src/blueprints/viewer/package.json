{
  "name": "blueprints-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild main.js --bundle --format=iife --global-name=BlueprintsViewerBundle --outfile=viewer.iife.js --minify"
  },
  "dependencies": {
    "three": "0.172.0"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  }
}
