{
  "name": "Blueprints",
  "install": "python3 -m venv .venv && . .venv/bin/activate && pip install -U pip && pip install -e .",
  "terminals": [
    {
      "name": "python",
      "command": "source .venv/bin/activate && bash"
    }
  ]
}
